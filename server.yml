http:
  rootPath: /service/*
defaultInterval: 60000
restFetchers:
  - host: http://localhost/metrics
    interval: 10000
    tags:
      application: 123
      host: localhost
      minTag: tada
      terje: erKul
  - host: http://localhost/metrics2