http:
  rootPath: /service/*
  port: 64000
defaultInterval: 60000
restFetchers:
  - host: http://localhost:2000/partsregister/admin/metrics
    interval: 10000
    tags:
      application: partsregister
      host: localhost
